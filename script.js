const displayController = () => {
    // updates display when player marks a cell
    
    // reset board when game over or click reset button
}

const gameBoard = () => {
    // tracks turns

    // detects for winner, loser, and draw
}

const Player = (name) => {

}